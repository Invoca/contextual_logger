---
apiVersion: v1
kind: Pod
metadata:
  labels:
    jenkins/contextual-logger: 'true'
  namespace: jenkins
  name: contextual_logger
spec:
  containers:
  - name: ruby
    image: ruby:2.6.5
    tty: true
    resources:
      requests:
        memory: "100Mi"
    command:
      - cat
